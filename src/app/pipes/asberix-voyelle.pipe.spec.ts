import { AsberixVoyellePipe } from './asberix-voyelle.pipe';

describe('AsberixVoyellePipe', () => {
  it('create an instance', () => {
    const pipe = new AsberixVoyellePipe();
    expect(pipe).toBeTruthy();
  });
});
